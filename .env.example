# Копировать в .env и подставить значения. Файл .env не коммитить.

# iiko Server (Resto) API — см. docs/iiko-server-api.md
IIKO_BASE_URL=https://oreks-co.iiko.it:443
IIKO_LOGIN=
# Пароль: либо в открытом виде (IIKO_PASSWORD), либо SHA1-хеш (IIKO_PASSWORD_SHA1) — зависит от сервера
IIKO_PASSWORD=
# IIKO_PASSWORD_SHA1=
# Путь авторизации, если не /api/auth (например /resto/api/auth)
# IIKO_AUTH_PATH=/api/auth

# Neon Postgres (connection string из консоли Neon)
NEON_DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
