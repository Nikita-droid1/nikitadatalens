# Чеклист настройки проекта

## ✅ ШАГ 0: Добавление кода в GitHub

**Что значит "закоммитить"?** Сохранить код в Git и отправить на GitHub. Подробно: `docs/git_basics.md`

- [ ] Создал репозиторий на GitHub (или использую существующий)
- [ ] Закоммитил и запушил весь код проекта в репозиторий (через терминал, GitHub Desktop или VS Code)
- [ ] Проверил, что файл `.github/workflows/daily_etl.yml` есть в репозитории
- [ ] Убедился, что файл `.env` НЕ попал в репозиторий (он в `.gitignore`)

**Как проверить:** Откройте репозиторий на GitHub → вкладка **Actions** → должен появиться workflow "Daily ETL"

---

## ✅ ШАГ 1: GitHub Secrets

- [ ] Открыл репозиторий на GitHub
- [ ] Перешел: Settings → Secrets and variables → Actions
- [ ] Добавил секрет `IIKO_BASE_URL` = `https://oreks-co.Iiko.it:443`
- [ ] Добавил секрет `IIKO_LOGIN` = `UKK`
- [ ] Добавил секрет `IIKO_PASSWORD_SHA1` = `2c61422714bcb5935215adf442a8f5b1d80ffc54`
- [ ] Создал проект в Neon и получил Connection string
- [ ] Добавил секрет `NEON_DATABASE_URL` = `postgresql://...`
- [ ] (Опционально) Добавил секрет `GOOGLE_SHEETS_CREDENTIALS` если используете Google Sheets (см. `docs/google_sheets_setup.md`)

---

## ✅ ШАГ 2: Neon - Создание БД

- [ ] Создал проект в [neon.tech](https://neon.tech)
- [ ] Скопировал Connection string (для шага 1)
- [ ] Открыл SQL Editor в Neon
- [ ] Выполнил `neon/schema/001_iiko_raw.sql` (таблицы для iiko)
- [ ] Выполнил `neon/schema/002_sheets_raw.sql` (таблицы для Google Sheets)
- [ ] Выполнил `neon/schema/003_mart.sql` (витрина для DataLens)
- [ ] Проверил, что таблицы созданы (можно через SQL: `\dt` или `SHOW TABLES`)

---

## ✅ ШАГ 3: Тестовый запуск (ручной)

- [ ] Открыл GitHub → Actions → "ETL (ручной запуск)"
- [ ] Нажал "Run workflow" → выбрал ветку `main` → "Run workflow"
- [ ] Дождался завершения (1-2 минуты)
- [ ] Проверил логи:
  - [ ] Видно "Token получен"
  - [ ] Видно "Загружено X строк" для каждого отчета
  - [ ] Нет ошибок (красных сообщений)

---

## ✅ ШАГ 4: Проверка данных в Neon

- [ ] Открыл Neon SQL Editor
- [ ] Выполнил: `SELECT COUNT(*) FROM iiko_raw_margin;` (должно быть > 0)
- [ ] Выполнил: `SELECT COUNT(*) FROM mart_daily_metrics;` (должно быть > 0)
- [ ] Выполнил: `SELECT * FROM mart_daily_metrics ORDER BY report_date DESC LIMIT 5;` (вижу данные)

---

## ✅ ШАГ 5: Настройка DataLens

- [ ] Открыл [DataLens](https://datalens.yandex.ru)
- [ ] Создал подключение → PostgreSQL
- [ ] Вставил Connection string из Neon
- [ ] Протестировал подключение (✅ успешно)
- [ ] Создал датасет на основе `mart_daily_metrics`
- [ ] Создал датасет на основе `mart_hourly_load`
- [ ] Создал датасет на основе `mart_discount_types`
- [ ] Создал дашборд с 15 метриками (см. `docs/datalens_setup.md`)

---

## 🎉 Готово!

После выполнения всех шагов:
- ✅ ETL запускается вручную через GitHub Actions (кнопка "Run workflow")
- ✅ При запуске данные загружаются из iiko Server API в Neon
- ✅ Витрина автоматически обновляется после каждой загрузки
- ✅ DataLens показывает данные из Neon (обновляй вручную при необходимости)

---

## Если что-то не работает

1. **Проверь логи GitHub Actions** — там видно точную ошибку
2. **Проверь секреты** — все ли правильно добавлены
3. **Проверь Neon** — доступна ли БД, созданы ли таблицы
4. **Проверь формат данных** — возможно, iiko API возвращает данные в другом формате (смотри `raw_data` в таблицах)
