# Следующие шаги после загрузки в GitHub

## ✅ ШАГ 0 выполнен: Код загружен в GitHub

Теперь нужно выполнить оставшиеся шаги из чеклиста.

---

## 📋 ШАГ 1: Добавить секреты в GitHub

**Цель:** Добавить все учетные данные для работы ETL.

1. Открой свой репозиторий на GitHub
2. Перейди: **Settings** → **Secrets and variables** → **Actions**
3. Нажми **New repository secret**
4. Добавь следующие секреты (значения бери из своего `.env`):

| Имя секрета | Значение |
|-------------|----------|
| `IIKO_BASE_URL` | `https://oreks-co.Iiko.it:443` |
| `IIKO_LOGIN` | `UKK` |
| `IIKO_PASSWORD_SHA1` | `2c61422714bcb5935215adf442a8f5b1d80ffc54` |
| `NEON_DATABASE_URL` | *(получишь из Neon на следующем шаге)* |
| `GOOGLE_SHEETS_CREDENTIALS` | *(опционально, если используешь Google Sheets)* |

**Проверка:** Все 4-5 секретов добавлены в GitHub.

---

## 📋 ШАГ 2: Создать БД в Neon

**Цель:** Создать базу данных и выполнить SQL схемы.

### 2.1. Создание проекта в Neon

1. Зайди на [neon.tech](https://neon.tech)
2. Создай новый проект
3. **Скопируй Connection string** (будет нужен для шага 1)
   - Формат: `postgresql://user:password@host/db?sslmode=require`

### 2.2. Выполнение SQL схем

1. Открой **SQL Editor** в Neon
2. Выполни файлы **по порядку** (копируй содержимое из папки `neon/schema/`):

   **Файл 1:** `001_iiko_raw.sql`
   - Создает таблицы для сырых данных из iiko API

   **Файл 2:** `002_sheets_raw.sql`
   - Создает таблицы для данных из Google Sheets

   **Файл 3:** `003_mart.sql`
   - Создает витрину данных для DataLens

**Проверка:** В Neon созданы таблицы:
- `iiko_raw_margin`, `iiko_raw_load_orders`, `iiko_raw_load_revenue`, `iiko_raw_discount_types`
- `sheets_raw_direct`, `sheets_raw_fot`
- `mart_daily_metrics`, `mart_hourly_load`, `mart_discount_types`

---

## 📋 ШАГ 3: Тестовый запуск

**Цель:** Проверить, что выгрузка данных работает.

1. Открой GitHub → **Actions** → **"ETL (ручной запуск)"**
2. Нажми **Run workflow** → выбери ветку `main` → **Run workflow**
3. Дождись завершения (1-2 минуты)
4. Проверь логи:
   - ✅ Должно быть: "Token получен", "Загружено X строк"
   - ❌ Если ошибка — проверь секреты и логи

**Проверка:** В логах Actions видно успешную загрузку данных.

---

## 📋 ШАГ 4: Проверка данных в Neon

**Цель:** Убедиться, что данные загрузились в БД.

1. Открой Neon SQL Editor
2. Выполни запросы:

```sql
-- Проверка загрузки данных из iiko
SELECT COUNT(*) FROM iiko_raw_margin;
SELECT COUNT(*) FROM iiko_raw_load_orders;
SELECT COUNT(*) FROM iiko_raw_load_revenue;
SELECT COUNT(*) FROM iiko_raw_discount_types;

-- Проверка витрины (после трансформаций)
SELECT COUNT(*) FROM mart_daily_metrics;
SELECT * FROM mart_daily_metrics ORDER BY report_date DESC LIMIT 5;
```

**Проверка:** В таблицах есть данные.

---

## 📋 ШАГ 5: Настройка DataLens

**Цель:** Подключить DataLens к Neon и создать дашборд.

1. Открой [DataLens](https://datalens.yandex.ru)
2. Создай новое подключение → **PostgreSQL**
3. Вставь **Connection string** из Neon
4. Протестируй подключение
5. Создай датасеты на основе таблиц `mart_*` (см. `docs/datalens_setup.md`)
6. Создай дашборд с 15 метриками

---

## 🎯 Текущий статус

- ✅ Код загружен в GitHub
- ⏳ Нужно добавить секреты (ШАГ 1)
- ⏳ Нужно создать БД в Neon (ШАГ 2)
- ⏳ Нужно протестировать запуск (ШАГ 3)

**Следующий шаг:** Добавь секреты в GitHub (ШАГ 1).
